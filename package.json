{
    "name": "teamcity_project",
    "private": true,
    "version": "0.0.0",
    "type": "commonjs",
    "scripts": {
        "dev": "npx nodemon src/server.ts",
        "build": "npx tsc --build --clean",
        "start": "npm run build && node dist/server.js",
        "run": "npm run build && node dist/server.js",
        "test": "npx tsc && npx mocha ./dist/test"
    },
    "devDependencies": {
        "@types/express": "^4.17.17",
        "@types/mocha": "^10.0.1",
        "mocha": "^10.2.0",
        "nodemon": "^2.0.22",
        "ts-node": "^10.9.1",
        "typescript": "^4.9.3"
    },
    "dependencies": {
        "express": "^4.18.2"
    }
}
